<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mobile.application3DE">

    <application
        android:name=".utilities.MultiLanguageApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MobApp">

        <activity
            android:name=".ResultActivity"
            android:label="@string/title_activity_result"
            android:theme="@style/Theme.MobApp.NoActionBar"></activity>
        <activity android:name=".ProgressActivity" />
        <activity
            android:name=".LevelMusicPlayActivity"
            android:label="@string/title_activity_level_music_play"
            android:theme="@style/Theme.MobApp.NoActionBar" />
        <activity android:name=".SpeechTestActivity" />
        <!-- check if largeHeap is ok to use -->
        <activity android:name=".orientation.TccImageLayout" />
        <activity android:name=".activity_choice_landing" />
        <activity android:name=".SignInPage"/>
        <activity android:name=".Landing">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".user_details_2" />
        <activity android:name=".orientation.attentionCalculation" />
        <activity android:name=".orientation.tccCalculation" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>